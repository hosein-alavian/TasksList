<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:title="http://schemas.android.com/tools">
    <item
        android:id="@+id/action_search"
        android:icon="@drawable/search_menu_item_image"
        android:orderInCategory="1"
        android:title="@android:string/search_go"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="always" />
    <item
        android:id="@+id/menu_user_profile"
        android:icon="@drawable/user_menu_image"
        android:title="Profile"
        app:showAsAction="always"
        android:orderInCategory="2">
        <menu>
            <item
                android:id="@+id/menu_exit_profile"
                android:icon="@drawable/exit_menu_image"
                android:title="Exit" />

        </menu>
    </item>

</menu>